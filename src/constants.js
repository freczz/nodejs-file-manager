const USERNAME_ARG_TEXT = '--username=';

const DEFAULT_USERNAME = 'user';

const INVALID_INPUT = 'Invalid input';

const OPERATION_FAILED = 'Operation failed';

const EXIT_COMMAND = '.exit';

const TYPE_NAMES = {
    directory: 'directory',
    file: 'file',
}

const OS_ARGS = {
    eol: '--EOL',
    cpus: '--cpus',
    homedir: '--homedir',
    username: '--username',
    architecture: '--architecture',
}

export {
    USERNAME_ARG_TEXT,
    DEFAULT_USERNAME,
    INVALID_INPUT,
    OPERATION_FAILED,
    EXIT_COMMAND,
    TYPE_NAMES,
    OS_ARGS,
}
